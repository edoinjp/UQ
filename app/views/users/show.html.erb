<div class="user-profile">
  <div class="user-details">
    <h2><%= @user.first_name %>'s Profile</h2>
    <p><strong>Preferred Learning Style:</strong> <%= @user.learning_style %></p>
  </div>

  <div class="lesson-quiz-results">
    <h3>Lesson: <%= @lesson.title %></h3>
    <p>Quiz Score: <%= @quiz_score %> out of 5</p>
    <!-- replace @lesson and @quiz_score with actual data later -->
  </div>
</div>
